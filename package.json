{
  "name": "chart-img-telegram-bot",
  "version": "1.0.0",
  "description": "chart-img telegram bot",
  "author": "CHART-IMG <developer@chart-img.com>",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "npx wrangler dev --local --ip 0.0.0.0",
    "cf-dev": "npx wrangler dev",
    "cf-publish": "npx wrangler publish",
    "cf-publish-production": "npx wrangler publish --env production",
    "prettier": "prettier --write '**/*.{js,json,md}'"
  },
  "dependencies": {
    "itty-router": "^3.0.11",
    "query-string": "^8.1.0",
    "winston": "^3.8.2"
  },
  "devDependencies": {
    "prettier": "^2.8.4",
    "wrangler": "^2.12.2"
  }
}
