{
  "name": "chart-img-telegram-bot",
  "version": "1.0.0",
  "description": "chart-img telegram bot",
  "author": "CHART-IMG <developer@chart-img.com>",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "npx wrangler dev --local --ip 127.0.0.1 --port 8080",
    "ngrok": "node src/setup/ngrok --port 8080",
    "setup": "node src/setup/telegram",
    "publish-cf": "npx wrangler publish",
    "prettier": "prettier --write '**/*.{js,json,md}'"
  },
  "dependencies": {
    "itty-router": "^3.0.11",
    "query-string": "^8.1.0",
    "winston": "^3.8.2"
  },
  "devDependencies": {
    "args": "^5.0.3",
    "inquirer": "^9.1.4",
    "ngrok": "^4.3.3",
    "prettier": "^2.8.4",
    "wrangler": "^2.12.3"
  }
}
